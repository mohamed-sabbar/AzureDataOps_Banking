trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: variable group   # ton groupe de variables Azure DevOps

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.10'

- script: |
    pip install -r requirements.txt
    pip install databricks-cli
  displayName: 'Installer dépendances'

- script: |
    export DATABRICKS_HOST=https://adb-1119135389290183.3.azuredatabricks.net
    export DATABRICKS_TOKEN=$(DATABRICKS_TOKEN)
    
    databricks workspace import_dir notebooks /Workspace/Users/mohamedazrou2003@gmail.com --overwrite
    echo "✅ Notebooks importés dans Databricks"
  displayName: 'Déployer notebooks Databricks'

- script: |
    databricks jobs run-now --job-id 89  # remplacer par ton ID_JOB réel
    echo "🚀 Job Databricks exécuté"
  displayName: 'Exécuter job Databricks'
